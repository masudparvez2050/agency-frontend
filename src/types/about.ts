export interface TeamMember {
  name: string;
  role: string;
  bio: string;
  github: string;
  avatarInitials: string;
  accent: string;
}

export interface CompanyValue {
  title: string;
  description: string;
  iconName: string;
}
